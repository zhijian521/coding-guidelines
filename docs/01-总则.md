# 一、总则

## 1.1 适用范围

- Web 前端项目
- Vue / React 项目
- JavaScript / TypeScript 项目
- 页面开发、组件开发、重构、联调、代码评审

新项目从一开始就按这套规范走。老项目不要求一次性改完，但每次改到相关模块时顺手对齐。规范冲突时，以本文档为准——如果确实有更好的做法，在 Code Review 里说明理由即可。

## 1.2 核心原则

### 可读优先

代码是写给人看的，顺便让机器执行。如果你写完三个月后自己都读不懂，那这段代码就有问题。变量名、函数名必须说清楚自己是干什么的，`data`、`temp`、`item1` 这种名字一律不给过。

### 一致至上

项目里所有代码应该像同一个人写的。缩进用 2 空格还是 4 空格不重要，重要的是别一个文件 2 空格、另一个文件 Tab。新加入的同事能快速上手，不会因为风格不一致浪费时间。

### 极简原则

没用的代码直接删掉，包括注释掉的旧代码、废弃的分支逻辑、写了但没人调的函数。不要因为"以后可能用得上"留着——Git 历史能找回来。也别为了"设计模式"引入不必要的抽象层，一个页面才 200 行代码，不需要拆 5 层。

### 渐进收敛

新项目严格按规范来。老项目每次改到相关模块时顺便对齐，不要让"历史遗留"成为永远的借口。半年后回头看，大部分代码应该已经收敛了。

## 1.3 禁止项

- 为了"看起来高级"引入不必要的抽象
- 简单需求拆出过多文件和中间层
- 临时方案（workaround）长期留在线上代码中
- 复制旧代码后只改局部变量名，保留无用的冗余结构

> **实际场景：** 见过一个表单页面，总共 3 个输入框，被拆了 8 个组件、4 个 composable、2 个 store 模块。后来要改一个校验逻辑，得在 6 个文件之间跳来跳去。这不是工程化，这是过度设计。

## 1.4 文档使用说明

规范发生冲突时，以本文档为准；确有更好的做法，在 MR 中说明理由即可。规范本身也会持续迭代——如果你发现某条规则在项目中反复被绕过，说明规则本身需要讨论修改，提 issue 或团队周会讨论。
